@import 'nib'
@import '../variables'

#games
  margin 60px 0 0 0
.game
  minSize(360,400)
  size(360,400)
  maxSize(360,400)
  box-shadow 0 0 2px midnight(0.3)
  margin 20px 10px 0 10px
  cursor pointer
  float left
  padding 10px
.gameTitle
  font-size 18px
.gameContent
  font-size 14px
