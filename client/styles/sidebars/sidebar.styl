@import 'nib'
@import '../variables'

.sidebar
  minSize(360)
  size(360)
  maxSize(360)
  whiteTheme(0.95)
  box-shadow 0 0 2px midnight(0.3)
  z-index 1
  display none
  &.left
    position(fixed,80,auto,20,0)
  &.right
    position(fixed,80,0,20,auto)